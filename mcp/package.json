{
  "name": "actual-budget-mcp",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "The actual_budget MCP server. Ours, never published. See pm/mcp.mdx.",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "node scripts/build-instructions.mjs && tsc -p tsconfig.json && node scripts/stamp.mjs",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest --run"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "1.30.0",
    "zod": "4.4.3"
  }
}
