---
category: Features
authors: [BryanStarbuck]
---

Add a local API for the `abx` command line tool and the Actual Budget MCP server, so both can report which budget is open, whether the app is ready to answer questions, and what the server build supports.
